import { JSX } from "react";
//this component does nothing but retrieve property shared data used on all property child pages so that it is available in context.
const PropertySharedData = (): JSX.Element => {
  return <></>;
};
export default PropertySharedData;
